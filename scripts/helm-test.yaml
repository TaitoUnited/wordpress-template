# values for chart https://hub.kubeapps.com/charts/stable/wordpress
wordpress:
  ingress:
    hosts:
      # TODO get default host from taito-config.sh
      - name: wordpress-template-test.taitodev.com
        tlsSecret: wordpress-template-test-tls-secret
        tls: false # TODO true
    annotations:
      # TODO remove ENV from helm release name
      ingress.kubernetes.io/auth-secret: wordpress-template-test-basic-auth
