# values for chart https://hub.kubeapps.com/charts/stable/wordpress
# TODO get environment specific settings from taito-config.sh
wordpress:
  replicas: 1
  ingress:
    hosts:
      - name: wordpress-template-test.taitodev.com
        tlsSecret: wordpress-template-test-tls-secret
        tls: true
        secureBackendDisabled: true
        annotations:
          kubernetes.io/ingress.class: nginx
          ingress.kubernetes.io/proxy-body-size: 1m
          kubernetes.io/tls-acme: "true"
          ingress.kubernetes.io/ssl-redirect: "true"
          ingress.kubernetes.io/force-ssl-redirect: "true"
          ingress.kubernetes.io/auth-type: "basic"
          ingress.kubernetes.io/auth-realm: wordpress-template
          ingress.kubernetes.io/auth-secret: wordpress-template-test-wp-basic-auth
  externalDatabase:
    host: mysql-sqlproxy-gcloud-sqlproxy.sqlproxy
    port: 3306
    database: wptemplatetest
    # wordpress creates tables in dev so we have to use the mgr user account
    # TODO tables are migrated -> use the app account here?
    user: wptemplatetest
    passwordSecretName: db.wptemplatetest.build
    passwordSecretKey: SECRET

  # smtpHost:
  # smtpProtocol:
  # smtpPort:
  # smtpUser:
  # smtpSecretName: smtp.wordpress-template-dev.email
  # smtpSecretKey: SECRET
  # smtpUsername:
