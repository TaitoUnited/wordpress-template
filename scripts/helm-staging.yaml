# values for chart https://hub.kubeapps.com/charts/stable/wordpress
wordpress:
  ingress:
    hosts:
      # TODO get default host from taito-config.sh
      - name: wordpress-template-staging.taitodev.com
        tlsSecret: wordpress-template-staging-tls-secret
        tls: true
        annotations:
          ingress.kubernetes.io/proxy-body-size: 1m
          ingress.kubernetes.io/ssl-redirect: 'true'
          ingress.kubernetes.io/force-ssl-redirect: 'true'
          ingress.kubernetes.io/auth-type: "basic"
          ingress.kubernetes.io/auth-realm: wordpress-template
          # TODO remove ENV from helm release name
          ingress.kubernetes.io/auth-secret: wordpress-template-staging-wp-basic-auth
  externalDatabase:
    # wordpress creates tables so we have to use the mgr user account
    database: wptemplatestag
    user: wptemplatestag
