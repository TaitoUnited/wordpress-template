# values for chart https://hub.kubeapps.com/charts/stable/wordpress
# TODO get environment specific settings from taito-config.sh
# --> no need for a separate file for each environment
wordpress:
  replicas: 2
  # TODO default for prod: 512Mi, 300m
  resources:
    requests:
      memory: 256Mi
      cpu: 100m
  persistence:
    size: 8Gi
  ingress:
    hosts:
      - name: wordpress-template-prod.taitodev.com
        tlsSecret: wordpress-template-prod-tls-secret
        tls: true
        secureBackendDisabled: true
        annotations:
          kubernetes.io/ingress.class: nginx
          ingress.kubernetes.io/proxy-body-size: 1m
          kubernetes.io/tls-acme: "true"
          ingress.kubernetes.io/ssl-redirect: "true"
          ingress.kubernetes.io/force-ssl-redirect: "true"
          ingress.kubernetes.io/auth-type: "basic"
          ingress.kubernetes.io/auth-realm: wordpress-template
          ingress.kubernetes.io/auth-secret: wordpress-template-prod-wp-basic-auth
  externalDatabase:
    host: mysql-sqlproxy-gcloud-sqlproxy.sqlproxy
    port: 3306
    database: wptemplateprod
    # wordpress creates tables in dev so we have to use the mgr user account
    # TODO tables are migrated from previous env -> use the app account here?
    user: wptemplateprod
    passwordSecretName: db.wptemplateprod.build
    passwordSecretKey: SECRET

  # smtpHost:
  # smtpProtocol:
  # smtpPort:
  # smtpUser:
  # smtpSecretName: smtp.wordpress-template-dev.email
  # smtpSecretKey: SECRET
  # smtpUsername:
