{
  "name": "wordpress-template",
  "scripts": {
    "install-ci": "echo '# No additional dependencies need to be installed in ci mode'",
    "install-dev": "echo '# No additional dependencies need to be installed'",
    "taito-init:local": "echo Nothing to initialize. All 'database/init/*.sql' scripts will be run on 'taito start --clean'.",
    "taito-db-dump:local": "taito db-dump:local ./database/init/init.sql",
    "show-basic-auth": "echo * Non-production basic auth credentials: TODO user / painipaini",
    "info:local": "echo 'echo * Shared user accounts are located in a shared password manager && echo * Admin user for local database: admin / pass'",
    "info:dev": "npm run show-basic-auth && echo Shared user accounts are located in a shared password manager",
    "info:prod": "echo Shared user accounts are located in a shared password manager",
    "sync-to": "echo TODO git pull --rebase && sync data/files to ${taito_env}",
    "sync-from": "echo TODO git pull --rebase && sync data/files from ${taito_env}",
    "sync-to:local": "echo Give environment",
    "sync-from:local": "echo Give environment",
    "project-contacts": "cat PROJECT.md | sed -n -e '/^## Contacts.*$/,/^#.*$/{ /^## Contacts.*$/d; /^#.*$/d; p; }'",
    "commit": "echo 'Running commitizen locally' && git-cz",
    "commitmsg": "echo 'NOTE: Running validate-commit-msg locally' && validate-commit-msg",
    "release-pre:prod": "semantic-release pre",
    "release-post:prod": "semantic-release post"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    },
    "validate-commit-msg": {
      "types": [
        "wip",
        "feat",
        "fix",
        "docs",
        "style",
        "refactor",
        "perf",
        "test",
        "build",
        "ci",
        "chore",
        "revert"
      ],
      "scope": {
        "required": false,
        "allowed": [
          "*"
        ],
        "validate": false,
        "multiple": false
      },
      "warnOnFail": false,
      "maxSubjectLength": 100,
      "subjectPattern": ".+",
      "subjectPatternErrorMsg": "subject does not match subject pattern!",
      "helpMessage": "",
      "autoFix": false
    }
  },
  "release": {
    "getLastRelease": "last-release-git",
    "debug": false,
    "verifyConditions": {
      "path": "./node_modules/semantic-release/src/lib/plugin-noop.js"
    }
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com:TaitoUnited/wordpress-template.git"
  },
  "devDependencies": {
    "commitizen": "^2.9.6",
    "cz-conventional-changelog": "^2.1.0",
    "husky": "^0.14.3",
    "last-release-git": "0.0.3",
    "npm-run-all": "^4.1.3",
    "semantic-release": "^8.1.2",
    "validate-commit-msg": "^2.14.0"
  }
}
