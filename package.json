{
  "name": "wordpress-template",
  "scripts": {
    "install-ci": "echo '# No additional dependencies need to be installed in ci mode'",
    "install-dev": "echo '# No additional dependencies need to be installed'",
    "taito-init:local": "echo Nothing to initialize. All 'database/init/*.sql' scripts will be run on 'taito start --clean'.",
    "taito-db-dump:local": "taito db-dump:local ./database/init/init.sql",
    "show-basic-auth": "echo '* Non-production basic auth credentials: TODO user / painipaini '",
    "info:local": "echo '* Shared user accounts are located in shared password manager' && echo '* Admin user for local database: admin / pass'",
    "info:stag": "npm run show-basic-auth && echo '* Shared user accounts are located in shared password manager'",
    "info:prod": "echo '* Shared user accounts are located in shared password manager'",
    "sync-to": "echo TODO git pull --rebase && sync data/files to ${taito_env}",
    "sync-from": "echo TODO git pull --rebase && sync data/files from ${taito_env}",
    "sync-to:local": "echo Give environment",
    "sync-from:local": "echo Give environment",
    "project-contacts": "cat PROJECT.md | sed -n -e '/^## Contacts.*$/,/^#.*$/{ /^## Contacts.*$/d; /^#.*$/d; p; }'",
    "commitmsg": "commitlint -E GIT_PARAMS",
    "precommit": "if [[ $taito_mode != 'ci' ]]; then echo No pre-commit validations; fi",
    "prepush": "if [[ $taito_mode != 'ci' ]]; then echo No pre-push validations; fi",
    "release-pre:prod": "semantic-release --dry-run",
    "release-post:prod": "semantic-release --no-ci"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ],
    "rules": {
      "type-enum": [
        2,
        "always",
        [
          "wip",
          "feat",
          "fix",
          "docs",
          "style",
          "refactor",
          "perf",
          "test",
          "revert",
          "build",
          "ci",
          "chore"
        ]
      ]
    }
  },
  "release": {
    "npmPublish": false,
    "analyzeCommits": {
      "preset": "angular",
      "releaseRules": [
        {
          "type": "feat",
          "release": "minor"
        },
        {
          "type": "fix",
          "release": "patch"
        },
        {
          "type": "docs",
          "release": "patch"
        },
        {
          "type": "style",
          "release": "patch"
        },
        {
          "type": "refactor",
          "release": "patch"
        },
        {
          "type": "perf",
          "release": "patch"
        },
        {
          "type": "test",
          "release": "patch"
        },
        {
          "type": "revert",
          "release": "patch"
        },
        {
          "type": "build",
          "release": "patch"
        },
        {
          "type": "ci",
          "release": "patch"
        },
        {
          "type": "chore",
          "release": "patch"
        }
      ]
    }
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TaitoUnited/wordpress-template.git"
  },
  "devDependencies": {
    "@commitlint/cli": "^7.1.2",
    "@commitlint/config-conventional": "^7.1.2",
    "git-branch-is": "~2.0.0",
    "husky": "~0.14.3",
    "npm-run-all": "~4.1.3",
    "semantic-release": "^15.9.15"
  }
}
